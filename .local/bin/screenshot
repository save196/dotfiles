#!/bin/bash

if [ -z "$1" ]; then
    import -window root "$HOME$(date +'/Pictures/Screenshot from %Y-%m-%d %H-%M-%S.png')"
    exit
fi

import "$HOME$(date +'/Pictures/Screenshot from %Y-%m-%d %H-%M-%S.png')"
